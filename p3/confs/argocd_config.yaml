apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app
  namespace: argocd
spec:
  destination:
    namespace: dev
    server: https://kubernetes.default.svc
  project: default
  source:
    path: .
    repoURL: https://github.com/lekix-lekix/IOT_PART3_APP_MAMOULIN.git
    targetRevision: main
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true
    # syncOptions:
      # - CreateNamespace=true